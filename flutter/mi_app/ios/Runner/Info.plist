<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <!-- Región y configuración base -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>

    <!-- Ejecutable principal -->
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <!-- Identificador del bundle -->
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <!-- Info dictionary -->
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- Nombre interno de la app -->
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>

    <!-- Tipo de paquete -->
    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <!-- Versión -->
    <key>CFBundleShortVersionString</key>
    <string>1.0.0</string>

    <key>CFBundleSignature</key>
    <string>????</string>

    <key>CFBundleVersion</key>
    <string>1</string>

    <!-- Requerido por iOS -->
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- Pantalla de lanzamiento -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <!-- Storyboard principal (NECESARIO PARA QUE NO SE QUEDE EN NEGRO) -->
    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <!-- Capacidades del dispositivo -->
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>

    <!-- Orientaciones soportadas -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Deep Link Scheme: carlitostravel:// -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>carlitostravel</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>carlitostravel</string>
            </array>
        </dict>
    </array>

    <!-- Universal Links (si algún día los usas) -->
    <key>NSUserActivityTypes</key>
    <array>
        <string>NSUserActivityTypeBrowsingWeb</string>
    </array>

    <!-- Requerido por plugins de Flutter -->
    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
</dict>
</plist>
